import BaseUnorderedList from '../base/base-ul';
import { styled } from 'Styles/stitches.config';

export const UnorderedList = styled('ul', BaseUnorderedList);
export const OrderedList = styled('ol', BaseUnorderedList);
